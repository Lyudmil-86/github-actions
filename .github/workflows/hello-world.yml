name: hello-world

on: push

jobs:
  hello-world-job:

    runs-on: ubuntu-latest
    
    steps:
      - name: check out repository
        uses: actions/checkout
      - run: echo "${cat hello_world.txt}"
      
      
    
